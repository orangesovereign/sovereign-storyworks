{
  "schema": 1,
  "code": "phase1_test",
  "title": "The Courier's First Ride",
  "start": "ride_out",
  "nodes": {
    "ride_out": {
      "type": "goto",
      "label": "Ride at least 60 meters away from where you started — any direction. The wire will call out your distance.",
      "config": { "departure": { "distance": 60.0 } },
      "onSuccess": "ride_back",
      "onFailure": "botched"
    },
    "ride_back": {
      "type": "goto",
      "label": "Return to where you began — the wire counts you down.",
      "config": { "relative": { "origin": true }, "radius": 6.0 },
      "onSuccess": "done",
      "onFailure": "botched"
    },
    "done": {
      "type": "end",
      "config": { "outcome": "completed", "message": "The courier's route is run. The county thanks you." }
    },
    "botched": {
      "type": "end",
      "config": { "outcome": "failed", "message": "The route went cold. Better luck next ride." }
    }
  }
}
